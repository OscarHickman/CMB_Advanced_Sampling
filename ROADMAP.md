# Research Roadmap: Differentiable Bayesian CMB Analysis

*Forward-looking plan only. Completed/closed-out work — including the full Phase 0b (CG) and Phase 0c (messenger-field) sagas — is summarised in `achievements.md`; full detail in git history. Restructured 2026-07-13.*

**The claim being built:** the first full-sky, curved-sky (HEALPix), differentiable joint Gibbs sampler over (alm_unlensed, C_ℓ, φ). Flat-sky joint sampling exists (CMBLensing.jl); full-sky methods are point-estimate or marginal (MUSE, QE). LiteBIRD-class delensing is structurally inaccessible to flat-sky codes. **The window is finite (curved-sky MUSE announced) — the alm-sampler fix and Phase 2 are the critical path; everything else waits.**

---

## 0. HMC + matrix-free-SHT pivot — VALIDATED 2026-07-13, root cause of the earlier bias found (not a bug)

The messenger-field route is closed (intrinsic critical slowing down at production scale — see `achievements.md`). The replacement: a one-line JIT-compile fix lets HMC finally run with the Phase-1.5 matrix-free SHT at **≈0.34-0.6s/sweep (~600-1000× cheaper than messenger's 354.68s)**.

- [x] **Fix committed** — `model.py::psi_tf` disables JIT when `use_matrixfree_sht=True` (XLA cannot compile through the `tf.py_function` ducc0 escape hatch). Committed `e99fdb2`.
- [x] **Burn-in-length investigation, several rounds:** `n_burnin=500` showed severe, unresolved drift at l≥200; `n_burnin=3000` fixed l≤150 (ESS 60-100%) but l=200-299 still drifted; a 4500-sweep full-trace run showed the drift *decelerating* late but not conclusively; `n_burnin=6000` (no MAP init) finally gave median ESS 80.6% with flat quartile means almost everywhere (l=250 a lone exception at 3.8% ESS) — **but its C_l came out systematically 3-6x LOW vs the trusted Phase 0 reference** (`results/lmax300_nside256_gibbs_real_double`), worse at higher l.
- [x] **That "3-6x low" result triggered a full matrix-free-SHT correctness audit — the SHT itself is exonerated.** `scripts/debug_matrixfree_sht_accuracy_production_scale.py`: forward synthesis vs `healpy.alm2map` at lmax=300/NSIDE=256, real mask — **5.7e-13 relative error** (machine precision). `scripts/debug_matrixfree_sht_gradient_production_scale.py`: custom-gradient backward pass vs finite differences at the same scale — **max 1.8e-7 relative error** (also machine precision; the lmax=15/lmax=12-only prior test coverage never actually contradicted this). The forward+backward SHT is bit-perfect at production scale.
- [x] **Actual root cause: an apples-to-oranges test setup, not a bug.** The trusted reference chain (`scripts/submit_gibbs_L300_double.slurm`) uses `--map_steps 2000 --map_lr 0.01 --n_lfs 20`; every ad-hoc benchmark chain this pass used `--map_steps 0 --n_lfs 10` — no MAP initialisation, so the chain has to climb from a naive start (psi≈5.0e9) to the true posterior mode (psi≈4.7e8) via burn-in alone, exactly matching the observed slow, l-dependent climb. **Re-run matching the reference's actual settings (`--map_steps 2000 --map_lr 0.01 --n_lfs 20`, `n_burnin=500`, `n_samples=1000`) matches the Phase 0 reference C_ℓ to 1-3% at every probed l (ratios 0.986-1.025, l=2 through 299)** — no bias, fully validated.
- [x] **Remaining, expected limitation: ESS at l=200-250 is still modest (2-4%) at only 1000 samples**, even with correct MAP init — matches Phase 0's own historical situation (their float64 baseline was accepted as "Phase 0 ✓" with similarly imperfect high-l ESS). Not a correctness problem — cheap to fix by running more samples (0.34-0.6s/sweep) if a specific analysis needs tighter high-l constraints.
- [x] **Multi-chain R-hat test — PASSED 2026-07-13.** 4 chains, `--map_steps 2000 --map_lr 0.01 --n_lfs 20`, `n_burnin=1500`, `n_samples=3000`, real Planck data, lmax=300 (job 11584886/87/88/89, `results/lmax300_nside256_gibbs_real_matrixfree_rhat/`). C_ℓ R-hat: max=1.030, median=1.005, 0% above 1.1 — **better than the Phase 0 dense-SHT reference itself** (max=1.014, median=1.001, computed on the same 4-chain reference with the same Gelman-Rubin code, but only 1000 samples/chain). alm-block R-hat is high in both (new: median=1.45, max=8.27, 83% >1.1; reference: median=1.60, max=7.13, 89% >1.1) — the pivot is *not worse* than the already-accepted Phase 0 baseline on this axis, consistent with `achievements.md`'s note that alm R-hat inflation at n≈90k is expected gradient-noise behaviour, not a regression.
- [x] **Tail-ESS check (Standing discipline: R-hat alone isn't enough).** Raw ESS on ln C_ℓ (TFP cross-chain ESS) is *lower per-sample* than the dense reference (new: median=31.4/12000 draws vs reference: median=129.8/4000 draws — roughly 4x worse sample efficiency, plausibly from the step-size adaptation settling higher: 0.157-0.163 vs reference's 0.105-0.113, with accept rates 0.55-0.65 vs 0.60-0.82). **But net throughput is what matters for production planning: ESS per wall-clock second is ~31x better for the pivot** (0.0182 ESS/s vs 0.00058 ESS/s), because the per-sweep cost dropped ~600-1000x while per-sample efficiency only dropped ~4x. Net verdict: gate passed; if a future analysis needs tighter per-sample efficiency, re-tune target-accept/step-size before scaling — cheap to iterate at 0.34-0.6s/sweep.
- [ ] **Optional follow-up (not blocking):** re-tune HMC target accept-rate/step-size-adaptation for the matrix-free path specifically — the reference's fixed/adapted step size regime (0.10-0.11, accept 0.6-0.82) mixes ~4x more efficiently per-sample than the pivot's (0.16, accept 0.55-0.65); recovering that would make the throughput win even larger. Skip unless Phase 2 chains show it matters.

## 1. Phase 2 — three-block Gibbs over (alm, C_ℓ, φ): the methods paper

Blocks: C_ℓ|alm exact inverse-Gamma (done); alm|C_ℓ,φ (the sampler above; note the conditional is *exactly Gaussian* at fixed φ — HMC is a cost choice, not a necessity, and the paper must say so); φ|alm,C_ℓ HMC (gradients fixed and production-trustworthy).

**Pre-production gates (cheap, do before any lmax=300 Phase 2 chain):**
- [x] **Joint (alm, φ) mixing check at lmax≤50 — MARGINAL, not pathological, 2026-07-17.** `scripts/gate_phi_alm_mixing_check.py`, job 11609120, lmax=50/nside=64, n_burnin=500/n_samples=3000, warm-started at truth. Worst ESS fraction 2.07% (alm block, l=21-30, IAT~44-48); phi block mixes well except l=2 (4.7% ESS). No Millea+2020-style catastrophic block-alternation collapse — comparable to the single-block HMC ESS already accepted in Phase 0 (l=200-250 at 2-4% ESS/1000 samples). Proceeding without a reparametrisation; re-check if Phase 2 lmax=300 chains show worse mixing than this scales to.
- [x] **Block-2 exactness experiment at small lmax — FAIL, 2026-07-18.** `scripts/gate_block2_exactness_check.py`, job 11610547, lmax=50/nside=64, n_burnin=500/n_samples=3000, warm-started at truth. Worst |fractional bias| in C_l^TT = 652% (l=13); pervasive across l=2-42 (5 worst multipoles all >275%, not one outlier), settling to <1% only near l=49. Weak lensing is *not* norm-preserving enough at this S/N for an unlensed-operator exact Gaussian Block-2 draw to be unbiased — keep HMC on both alm and phi blocks (gate 1's configuration, which passed MARGINAL). This shortcut is closed; do not revisit without a fundamentally different exact-draw calibration.

**The core deliverables:**
- [x] **Matrix-free port of the Block 3 lensing operator — done 2026-07-18.** `lens_map_tf`/`psi_lensed` (`lensing.py`) now branch on `model.use_matrixfree_sht` to use ducc0 full-sky synthesis (`sht_ducc.py::full_synthesis_tf`, new) instead of the dense `sph_parts` Y-matrix — this was the actual blocker for lmax=300 (gates 1/2 above only ran at lmax≤50 because Block 3 required dense SHT, exactly what the HMC pivot eliminated for scaling). Verified against the dense-SHT reference to numerical precision on a full-sky model (`tests/test_lensing.py::test_{lens_map_tf,psi_lensed}_matrixfree_matches_dense`). Not yet re-validated on a *masked* sky (all matrix-free lensing tests so far are full-sky).
- [x] **lmax=300 smoke run — PASS, 2026-07-18.** `scripts/smoke_lmax300_matrixfree_lensing.py`, job 11611338, lmax=300/nside=256, synthetic full-sky data, 20 burn-in + 50 samples, warm-started at truth. Finite samples/logp throughout; alm-block accept rate 0.860, phi-block accept rate 0.520 (both sane); 53.56s/sweep (much slower than the alm-only matrix-free pivot's 0.34-0.6s/sweep — expected, since each sweep now does an extra full-sky ducc0 synthesis for the phi/lensing block on top of the alm HMC steps). Port holds at production scale — proceed to full simulation validation.
- [ ] **Simulation validation — FAIL/INVESTIGATE, 2026-07-19.** `scripts/validate_sim_lmax300_lensing.py`, job 11612969 (lmax=300/nside=256, n_burnin=200, n_samples=1000, n_lfs=10, phi_n_lfs=10, warm-started at truth), completed in 9.65h (34.74s/collected-sample), mean alm accept=0.623, mean phi accept=0.738. Verdict: worst |z|=6.28, exceeds the 3σ point-agreement threshold. Root-caused (post-hoc analysis of `results/analysis/validate_sim_lmax300.npz`, not yet a re-run): **alm block** — frac_bias stays ≤0.5% at every l-bin but ESS is 0.7-4.3% (Sokal IAT estimator), consistent with gate 1's already-known ~2% ESS at this lmax; the large z there is very likely an ESS/mixing artifact inflating the posterior-std denominator, not a real bias. **phi block** — frac_bias is only 3-4%, but direct lag-1/lag-5 autocorrelation of the phi power is >0.9999 at every bin checked and the raw sample trace is a smooth near-monotonic drift, not noise around a mean — the Sokal estimator's ESS=100% reading is wrong here (it mishandles non-stationary/drifting series); the phi block is not actually mixing over the sampling phase, most likely because `phi_step≈6.4e-4` (final adapted step size) is too small to explore the posterior in the 1000-sample budget, so the point-agreement check is comparing against an unequilibrated phi chain. Needs: investigate why the phi step-size adaptation lands so small / whether phi needs a longer burn-in or a different leapfrog-count schedule, before re-submitting. Still owed beyond this: a full multi-realization rank/coverage test (~O(10) independent chains).
  - **Fix implemented, unit-tested, but 5-seed local smoke evidence is NOT favorable — 2026-07-20. NOT ready for an lmax=300 re-submission.** Root cause: `build_phi_prior_mass_sqrt` (`samplers.py`) preconditions the phi HMC block using only the prior curvature `1/cl_phiphi`, ignoring the lensing likelihood's curvature; at low L (likelihood-dominated) the true posterior is far tighter than the prior, so a single global step size settles at whatever that tight dimension needs and barely moves the rest. Added `lensing.py::estimate_phi_diag_fisher` (coordinate-sampled FD-of-analytic-gradient diagonal curvature estimate, validated against a brute-force dense Hessian reference at small lmax — an earlier joint-random-direction Hutchinson-probe design was tried and abandoned, see achievements.md) and `samplers.py::build_phi_posterior_mass_sqrt` (adds that curvature to the prior term, same pattern as the alm block's `Ninv_eff`); wired into `run_gibbs_chain` as opt-in `phi_mass_matrix='fisher'` (default remains `'prior'`, zero effect on existing callers, unit tests pass). **First single-seed smoke test looked promising** (lmax=12/nside=8: L=2 autocorrelation 0.54→0.015) but a follow-up single-seed test at lmax=16/nside=12 showed the opposite (0.37→0.64, worse). A 5-seed repeat at the original lmax=12/nside=8 scale resolved the disagreement unfavorably: L=2 autocorrelation prior→fisher was 0.815→0.503 (better), 0.122→0.304 (worse), 0.149→0.103 (better), 0.320→0.744 (worse), 0.507→0.824 (worse) — mean 0.383→0.495, i.e. worse on average, 3/5 seeds regressed. The one-shot coordinate-sampled Fisher curvature estimate (computed once, from a small `n_probes` sample, at one warm-up state) is a plausible suspect for this run-to-run instability and needs its own stability check before this mass-matrix design is trusted. Do not re-submit `validate_sim_lmax300_lensing.py` with `phi_mass_matrix='fisher'` until that's resolved.
- [x] **Masked-sky matrix-free lensing correctness — validated 2026-07-18.** `tests/test_lensing.py` (f_sky≈0.3 polar cap): `lens_map_tf`/`psi_lensed` match the dense reference at phi=0; at nonzero phi the dense path itself is not a valid masked-sky reference (zero-pads outside the observed footprint before deflection-interpolation — an artifact `full_synthesis_tf` was built to avoid), so validated instead by FD directly on the matrix-free path — passes. See `achievements.md`. Unblocks the patch-scale sanity check below.
- [ ] **CMBLensing.jl benchmark (mandatory — referees will demand it)**: matched flat-patch comparison vs its joint HMC and MUSE; full-sky comparison vs the quadratic estimator. **First**: a two-hour patch-scale f_sky≈0.016 sanity check of the sampler (all validation to date ran at f_sky≈0.7; masking pathologies grow as the mask deepens) — if it fails there, invert the benchmark design (full-sky vs QE as primary). Masked-sky matrix-free lensing correctness is now confirmed (above); this check is about mixing behaviour at a deep mask, not operator correctness — mirror `scripts/gate_phi_alm_mixing_check.py`'s structure with `model.unmasked_idx` overridden to a small polar cap (same construction as `tests/test_lensing.py::_polar_cap_mask_idx`) and `use_matrixfree_sht=True`. Not yet submitted — needs a SLURM job (~2h) and should be confirmed before submission.
- [ ] Quantify C_ℓ^TT bias reduction vs a lensing-blind Commander-style analysis of the same sims.
- [ ] Quantify what joint sampling buys over marginal methods (per-mode uncertainty propagation) — **as a figure, not a sentence**; this is the differentiator.
- [ ] Optional Block 4: C_ℓ^φφ|φ exact inverse-Gamma (same structure as Block 1).

## 2. The real-data run — Phase 2's science headline (⚠ REFRAMED 2026-07-13, see `literature.md`)

**The A_L anomaly is no longer live**: Planck PR4/NPIPE likelihoods (especially HiLLiPoP) report it much weakened/consistent with ΛCDM, and ACT DR6 lensing shows no excess. The honest pitch is now one of: (a) **the A_L post-mortem** — a joint (alm, C_ℓ, φ) posterior on Planck 2018 vs PR4 maps showing where in the joint space the anomaly lived and how it dissolves, the first fully-propagated-uncertainty account of its resolution; or (b) **internal-consistency machinery as the product** — the joint posterior as the principled lensing-consistency test for SO/LiteBIRD data, demonstrated on Planck. Phase 0 already runs on real Planck data, so marginal cost is small once Phase 2 validates.

- [x] **Pre-condition — beam + pixel window — done and validated 2026-07-18.** `power.py::beam_pixwin_transfer` wired into `CosmologyAdvancedSampling(..., beam_fwhm_arcmin=None)` (opt-in) and applied in `model.py::_psi_tf_raw` + `lensing.py::lens_map_tf`/`psi_lensed`; validated against an independent healpy ground truth with a negative control (see `achievements.md`). Full test suite (68 passed) confirms no regression to any existing (unbeamed) call site.
- [ ] **Pre-condition — anisotropic per-pixel noise N_ii (still open, blocks any real-data claim):** currently `Ninv` is a uniform scalar (`model.py`'s `_noisesig`); real Planck noise is spatially varying. Same diagonal-operator pattern as the beam above (multiply, not matrix), but lives in the *noise* term (`Ninv_masked`/`Ninv_parts`) rather than the signal path, so it's a separate, smaller change — thread a per-pixel `Ninv` map through the constructor instead of a scalar `_noisesig`, validated against a synthetic-noise reference the same way as the beam above.
- [ ] Run the joint sampler on real Planck data; report the joint (C_ℓ, φ) posterior's internal lensing-consistency verdict.

## 3. Phase 3 — polarization / LiteBIRD delensing (the science paper)

Full TQU joint analysis; the reason curved-sky matters at all. After Phase 2 submits. Target-experiment reference: the LiteBIRD lensing forecast (arXiv:2507.22618, July 2025) — its pipeline is QE/iterative, so a sampling-based delensing result fills a real gap, not a redundant one (see `literature.md`).

- [ ] Spin-2 extension of alm utilities and the lensing operator (ducc0 spin-2 transforms — Phase 1.5 infrastructure carries over).
- [ ] TQU joint likelihood with (TT, TE, EE, BB) block; C_ℓ^TE breaks inverse-Gamma conjugacy → 2×2 inverse-Wishart (BeyondPlanck structure) or HMC.
- [ ] Simulated lensed TQU at LiteBIRD-like noise: delensing efficiency vs QE/iterative baselines; recovered r constraint.

## 4. Parked (do not start; recorded so the platform argument isn't lost)

- **Phase 2b — ΛCDM parameters from C_ℓ**: scientifically routine; a cheap robustness section *after* the Phase 2 paper submits.
- **Phase 4 — lmax≥1000 scaling**: tuning, not rearchitecture, now the dense matrix is gone; profile only when Phase 2/3 need it.
- **Phase 5 — non-Gaussian extensions** (fNL sampling, mask in-painting, learned priors, systematics blocks): each a separate paper after Phases 2–3.
- **Lensed-operator exact Block-2 draw**: gate 2 (Section 1) failed because `sample_alm_cg` was calibrated against the *unlensed* operator `Y` while the data pass through `L(φ)∘Y` — the 652% bias is a calibration mismatch, not proof that no exact draw can work. A CG solve built from autodiff of `psi_lensed` (mirroring `_cg_grad_fn`'s use of `_psi_tf_raw`) would target the correct conditional exactly; the open question is whether the diagonal preconditioner still near-diagonalises `(L∘Y)^T N^{-1} (L∘Y)` well enough for fast PCG convergence, since lensing breaks the ℓ-diagonal structure the current preconditioner assumes. Not worth pursuing unless HMC-on-both-blocks (the current path) becomes a proven throughput bottleneck at lmax=300 — gate 1 passed MARGINAL, so no urgency.

## Standing discipline

- **One critical path**: HMC-pivot validation → Phase 2 gates → Phase 2 validation + paper. Anything not on it waits.
- **Precision rule**: fp64 end-to-end unless a mixed scheme with fp64 accumulation is validated against fp64 chains (the float32 false convergence is the standing counterexample).
- **Dense-reference discipline**: every new sampler/operator is validated against an exact small-scale reference before production — this has caught five real bugs so far (see `achievements.md`); don't skip it under time pressure.
- **Claims hygiene**: every "first" carries scope qualifiers and nearest-prior-work citations (Millea+2020, MUSE, delensalot); re-check arXiv for curved-sky MUSE before each submission milestone.
- **R-hat on C_ℓ alone is not convergence** — chains drifting together from the same start fool it; check the alm block and tail-ESS trends too (the messenger post-mortem's lesson).
- On dine2/cosma8 reused nodes, scripts need a job-private `$TMPDIR` (autograph cache collision).
- **`tf.gather` on a `tf.custom_gradient` output produces `IndexedSlices`, not a dense tensor**, as the upstream cotangent — any downstream `tf.py_function`/`.numpy()` call in that custom gradient's backward pass must `tf.convert_to_tensor()` it first (bit us in `apply_lensing_tf`'s backward when the matrix-free masked-likelihood path added a `tf.gather` on its output — see `lensing.py`, commit `19a7977`). Applies to any future custom-gradient op fed into `tf.gather`/`tf.boolean_mask`.
