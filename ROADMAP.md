# Research Roadmap: Differentiable Bayesian CMB Analysis

*Forward-looking plan only. Completed/closed-out work — including the full Phase 0b (CG) and Phase 0c (messenger-field) sagas — is summarised in `achievements.md`; full detail in git history. Restructured 2026-07-13; completed items condensed to pointers and scope re-set against the diffusion/score-based competition 2026-07-29. Audited against live job state, the code, and the citations 2026-07-29 (job status, per-node sweep costs, Block-4 filing, and the coverage test's equilibration prerequisite all corrected).*

**The claim being built:** the first full-sky, curved-sky (HEALPix), differentiable joint Gibbs sampler over (alm_unlensed, C_ℓ, φ). Flat-sky joint sampling exists (CMBLensing.jl); full-sky methods are point-estimate or marginal (MUSE, QE). LiteBIRD-class delensing is structurally inaccessible to flat-sky codes. **The window is finite (curved-sky MUSE announced) — the alm-sampler fix and Phase 2 are the critical path; everything else waits.** *(Open wording question, 2026-07-29: since Block 4 landed, the sampler actually spans (alm_unlensed, C_ℓ, φ, C_L^φφ). The claim above is deliberately left at the settled, review-survivable triple — widening it to name C_L^φφ is a positioning call to make deliberately at drafting time, together with `achievements.md`'s matching "Positioning (settled)" line, not drifted into.)*

**Scope re-set 2026-07-29 (after the literature rescan found the diffusion/score-based route unaddressed — see `literature.md`).** The competing paradigm markets uncorrelated samples in ~0.2s and explicitly discards the two things this project spent its budget building (a differentiable forward model, and a sampler). That reframes what this project's deliverables have to be:

- **The product is *demonstrated* exactness, not asserted exactness.** "HMC targets the true posterior asymptotically" is worth nothing to a referee if the φ block's own ESS is 2-5% and its lag-1 autocorrelation is >0.99. The diffusion papers will show calibration plots. **A convincing coverage/rank test is therefore the single highest-value remaining deliverable — higher than any additional scale.** Correspondingly: run it at a scale where the chain can actually equilibrate (lmax≈100-150), not at lmax=300 where it demonstrably cannot yet. A converged coverage test at lmax=128 is worth far more than a non-converged point check at lmax=300. This is a deliberate scope *reduction* that raises impact.
- **The differentiator is the joint (C_ℓ, C_L^φφ) posterior with propagated correlations** — an object neither MUSE (marginal), QE (point estimate), Commander (lensing-blind), nor the diffusion route (learned, no C_ℓ block) produces at all. Block 4 (completed 2026-07-29, and originally filed here as "optional, lowest priority") is what makes this a *figure* rather than a sentence. It is now strategically central, not optional.
- **Position vs learned inference is offensive, not defensive.** An exact sampler is the natural *reference standard against which amortised/learned posteriors are validated* — which is precisely the ANVIL/KARMA thesis (learned posteriors can be calibrated yet inaccurate). That turns the diffusion line from competitor into consumer of this work. Caveat to keep honest: this framing is only as strong as the scale at which we can produce converged reference posteriors — if that is lmax≈128 and the diffusion papers operate at lmax≫1000, say so plainly rather than overclaiming.
- **Deprioritised accordingly:** the CMBLensing.jl benchmark is a related-work obligation (cite published numbers, don't install Julia), not a science result; lmax scaling is *not* the route to impact and should not consume budget before the coverage test lands.

---

## 0. HMC + matrix-free-SHT pivot — VALIDATED 2026-07-13, fully closed out

Replaced the closed-out messenger-field route; unlocked HMC at ≈0.34-0.6s/sweep with the Phase-1.5 matrix-free SHT. No open items — full detail (multi-chain R-hat gate, tail-ESS check, the burn-in/SHT-audit false alarm) is in `achievements.md`. The one non-blocking follow-up (re-tune HMC step-size adaptation for the matrix-free path) is parked in Section 4.

## 1. Phase 2 — four-block Gibbs over (alm, C_ℓ, φ, C_L^φφ): the methods paper

Blocks: C_ℓ|alm exact inverse-Gamma (done); alm|C_ℓ,φ (the sampler above; note the conditional is *exactly Gaussian* at fixed φ — HMC is a cost choice, not a necessity, and the paper must say so); φ|alm,C_ℓ HMC (gradients fixed and production-trustworthy); C_L^φφ|φ exact inverse-Gamma (Block 4, done 2026-07-29 — opt-in `sample_cl_phiphi=True`, and what makes the Priority-2 joint-posterior figure possible).

**Pre-production gates (cheap, do before any lmax=300 Phase 2 chain):**
- [x] Joint (alm, φ) mixing check at lmax≤50 — MARGINAL, not pathological, 2026-07-17. See `achievements.md`.
- [x] Block-2 exactness experiment at small lmax — FAIL, 2026-07-18, closed (do not revisit without a fundamentally different exact-draw calibration). See `achievements.md`.

**The core deliverables:**
- [x] Matrix-free port of the Block 3 lensing operator — done 2026-07-18. See `achievements.md`.
- [x] lmax=300 smoke run — PASS, 2026-07-18. See `achievements.md`.
- [x] Block 4: C_L^φφ|φ exact inverse-Gamma — done and validated 2026-07-29. See `achievements.md`. (Originally filed under Priority 3 as "optional, lowest priority" until the 2026-07-29 scope re-set; it is what makes the Priority-2 joint-posterior figure possible, so it is a core deliverable, not an optional extra.)
- [ ] **Simulation validation — in progress, re-running with the leapfrog-schedule fix.** Original run (job 11612969, phi_n_lfs=10) FAILed its 3σ point-agreement check 2026-07-19 (worst |z|=6.28); root-caused to the phi block not equilibrating within the sampling window, not real bias (`achievements.md`). Two candidate fixes were investigated: a Fisher-informed phi mass matrix (tried, unfavorable across 5 seeds, closed) and lengthening the phi HMC trajectory via `phi_n_lfs` (favorable and robust across 5 seeds) — both in `achievements.md`. A local cost check found phi_n_lfs=80 costs ~185s/sweep (vs 44s at phi_n_lfs=10), so the reworked validation run needs ~40-62 CPU-hours total depending on node (see the per-node measurements below), i.e. several sequential 24h submissions (`scripts/submit_validate_sim_lmax300_phi80.slurm`, checkpointed).
  - Job 11635790 (first submission, phi_n_lfs=80) NODE_FAIL'd 2026-07-23 after 15h45m — cluster-side failure, not a code issue — with 100/1000 samples checkpointed (300 sweeps in 15h45m on node gc002 = **189s/sweep**, confirming the ~185s/sweep local estimate).
  - **Resubmitted 2026-07-29 as job 11661265** (node gc006), resuming from checkpoint via `run_gibbs_chain`'s checkpoint-path resume logic — verified working: the log reports "Resumed from checkpoint: 100/1000 samples collected" and the chain reached 150/1000 by 15:56. **Observed rate on this node is ~115-120s/sweep, ~1.6x faster than gc002's 189s/sweep** — per-sweep cost is materially node-dependent, so re-measure from the checkpoint mtimes rather than assuming either figure. At 120s/sweep this job reaches ~800/1000 within its 24h walltime and needs **1 more** resubmission; at 190s/sweep it reaches ~550 and needs 2 more. Budget 1-3 further 24h submissions, not a fixed 2-3.
  - **⚠ Honest risk, and the reason the coverage test below is now scoped lower rather than waiting on this.** The leapfrog fix is validated only at toy scale, and even there it moves mean lag-1 autocorrelation of phi power from 0.745 to 0.576 — an improvement that is robust (20/20 seed×multipole combinations) but *not* a solved-mixing number. There is a live possibility this run fails its point-agreement check again for the same reason as job 11612969. **If it does, do not immediately buy more sweeps at lmax=300** — that is the expensive branch and it has already failed once. Take the lmax≈128 coverage test below as the primary exactness evidence and report lmax=300 as a demonstrated-scale result with its mixing caveat stated plainly.
- [x] Masked-sky matrix-free lensing correctness — validated 2026-07-18. See `achievements.md`. Unblocks the patch-scale sanity check below.
- [x] Patch-scale (f_sky≈0.016) mixing sanity check — MARGINAL, not pathological, 2026-07-21. See `achievements.md`. (Its original verdict was "proceed to the CMBLensing.jl benchmark"; the 2026-07-29 scope re-set superseded that — the benchmark is now Priority 3 and this gate's output is *reused* by it rather than leading into it.)

**Priority 1 — the exactness evidence (highest-value remaining work; see the scope re-set at the top):**
- [ ] **Multi-realization rank/coverage test at lmax≈100-150 — THE headline validation.** ~O(10-20) independent simulated realizations, each a full 4-block chain warm-started away from truth, checking that the rank statistic of the true (C_ℓ, C_L^φφ, φ) within each posterior is uniform (Cook-Gelman-Rubin / simulation-based calibration). This is the deliverable that converts "asymptotically exact" from an assertion into the paper's central evidence, and it is the direct answer to the diffusion route's calibration plots. **Deliberately scoped below lmax=300**: at lmax≈128 the per-sweep cost drops by roughly an order of magnitude vs the 185s/sweep at lmax=300/phi_n_lfs=80, making O(10-20) independent chains affordable *and* letting each one actually equilibrate — both prerequisites for a coverage test to mean anything. Design it to reuse `scripts/validate_sim_lmax300_lensing.py`'s structure (realized-power comparison via `compute_sl_np`, not the CAMB ensemble mean — see the cosmic-variance bug in `achievements.md`) rather than inventing a new harness. Requires Block 4 on (`sample_cl_phiphi=True`) for the C_L^φφ rank statistic to exist at all — which also forces `phi_mass_matrix='prior'` (the two are mutually exclusive by design; fisher is closed anyway). O(10-20) concurrent chains sits comfortably under the 200-job `durham`/`dine2` cap (54 jobs queued as of 2026-07-29), so the cap is not a constraint here.
  - **Prerequisite gate, not just a cost measurement (added 2026-07-29).** A rank/coverage test on a chain that has not equilibrated produces *confidently wrong* uniformity plots — worse than no test, and this project has already been burned twice by exactly that failure mode (job 11612969's φ block never equilibrated; the Sokal IAT estimator and the `integrated_autocorr_time` zero-variance guard *both* reported near-perfect mixing on non-mixing φ chains — see `achievements.md`). So before spending on the ensemble: run **one** pilot chain at the candidate lmax, warm-started away from truth, and check φ equilibration by **direct lag-k autocorrelation of the φ power** (the diagnostic that caught the failure), not by any IAT/ESS estimator. Only launch the O(10-20) chains once that pilot shows a converged noisy trace rather than a monotonic drift. The cost-scaling measurement falls out of the same pilot run.

**Priority 2 — the differentiator figures (what the paper is *for*; both now unblocked by Block 4):**
- [ ] **The joint (C_ℓ^TT, C_L^φφ) posterior with its full correlation structure — as a figure.** The single object no competing method produces: MUSE is marginal, QE is a point estimate, Commander is lensing-blind, the diffusion route has no C_ℓ block. Now directly available since Block 4 samples C_L^φφ jointly (2026-07-29). This is the paper's reason to exist and should be its lead figure.
  - **⚡ SEQUENCING FIX (2026-07-30) — get this from the Priority-1 pilot chain. One run, two deliverables.** As written, Priority 1 precedes Priority 2. But this figure now needs **one chain with `sample_cl_phiphi=True` at any scale** — no coverage ensemble, no new code — and Priority 1's own prerequisite gate is *already* "run **one** pilot chain at the candidate lmax, warm-started away from truth." **They are the same job.** Configure the pilot at lmax≈128 with `sample_cl_phiphi=True` (which Priority 1 needs regardless, for the C_L^φφ rank statistic to exist at all) and it returns the φ-equilibration verdict, the cost-scaling numbers, **and this figure**, together.
  - **Why the order matters, and it is not tidiness.** The 2026-07-29 scope re-set bets the paper's impact on two propositions: that a converged coverage test is affordable (a cost question, already being measured) and that **the joint posterior is an object worth a figure — which is an unexamined assumption.** If its correlation structure is visually unremarkable at lmax≈128, the re-set needs revisiting *before* O(10–20) chains are funded. That is a one-chain question, not a twenty-chain one: the cheapest available test of the paper's central premise. Run it first.
  - Note **job 11661265 cannot supply this** — it is the lmax=300 `phi80` validation run and predates Block 4, so `sample_cl_phiphi` is off. Do not wait on it for the figure.
- [ ] **Quantify what joint sampling buys over marginal methods (per-mode uncertainty propagation) — as a figure, not a sentence.**
- [ ] **Quantify C_ℓ^TT bias reduction vs a lensing-blind Commander-style analysis of the same sims** — the "this changes an answer" result that turns a methods demo into a measurement claim.
- [ ] **Write the position vs learned/amortised inference explicitly into the paper** (intro paragraph + a subsection), per `literature.md`'s 2026-07-29 rescan: exactness, joint-posterior scope, no training set — and the offensive framing that an exact sampler is the reference standard for validating learned posteriors (cite ANVIL/KARMA). Not optional; it is the most likely referee question.

**Priority 3 — related-work obligation (scope down; not a science result):**
- [ ] **CMBLensing.jl benchmark — cite published numbers, do not install Julia.** Design researched 2026-07-29; full notes: `docs/notes/cmblensing_benchmark_notes.md`. Key finding: diffcmb's already-completed patch-scale gate (job 11626450, f_sky=0.016 ≈ 660 deg²) already matches CMBLensing.jl's own "BIG" config (650 deg², their reported practical ceiling for flat-sky HMC) — no patch redesign needed, direction is diffcmb full-sky sim → cut flat patch → compare. Proposed comparison set: C_ℓ^TT/C_ℓ^EE posterior recovery, C_L^φφ posterior recovery (headline plot), per-mode ESS/accept-rate table (not a sentence), wall-clock-per-effective-sample (computable from CMBLensing.jl's own published Table II — 19-50h/GPU, autocorr lengths 5-33 — without rerunning). CMBLensing.jl's f'-block CG is architecturally the *same* pattern diffcmb uses (phi drawn separately via HMC), not the CG-shortcut diffcmb rejected (652% bias, Section 1's Block-2 gate) — state this distinction explicitly in any paper text to avoid an apparent (but false) contradiction. MUSE is confirmed marginal-only (no joint posterior), consistent with existing positioning.
  - **Decided by the 2026-07-29 scope re-set:** cite published numbers, don't install Julia (their runs are 19-50h/GPU each, no Julia env exists here, GPU-queue access unchecked — and a peer-reviewed number for the same configuration class is adequate for a related-work comparison). Reserve a local install *only* if a referee explicitly demands a same-realization overlay.
  - **Caveats to state plainly in the paper rather than paper over:** (a) **T-only vs polarization-only** — CMBLensing.jl's examples are all QU; diffcmb is T-only until Phase 3, so this is a matched-patch/different-observable comparison. (b) **lmax mismatch** — diffcmb's validated scale is lmax=300 vs their l<3500; frame as a methodology/mixing comparison, not an information-content one. Do **not** inflate diffcmb's lmax to force a match — per the scope re-set, scale is not the route to impact. (c) Their fiducial cosmology differs slightly from `LCDM_PARAMS` (tau/omega_b/omega_c) — note it as a small confound when citing.

## 2. The real-data run — end-to-end demonstration (⚠ REFRAMED 2026-07-13; demoted from "science headline" 2026-07-29)

**The A_L anomaly is no longer live**: Planck PR4/NPIPE likelihoods (especially HiLLiPoP) report it much weakened/consistent with ΛCDM, and ACT DR6 lensing shows no excess. The honest pitch is now one of: (a) **the A_L post-mortem** — a joint (alm, C_ℓ, φ) posterior on Planck 2018 vs PR4 maps showing where in the joint space the anomaly lived and how it dissolves, the first fully-propagated-uncertainty account of its resolution; or (b) **internal-consistency machinery as the product** — the joint posterior as the principled lensing-consistency test for SO/LiteBIRD data, demonstrated on Planck. Phase 0 already runs on real Planck data, so marginal cost is small once Phase 2 validates.

**Why demoted (2026-07-29 scope re-set):** with the anomaly dead, a real-Planck joint posterior at lmax=300 T-only cannot carry a headline — it demonstrates the machinery end-to-end, which is worth doing and now cheap (both pre-conditions below are done), but the paper's impact rests on the Section 1 Priority-1/2 deliverables (coverage test, joint-posterior figures), not on this. Keep it, size it accordingly, and don't let it displace the coverage test.

- [x] Pre-condition — beam + pixel window — done and validated 2026-07-18. See `achievements.md`.
- [x] Pre-condition — anisotropic per-pixel noise N_ii — done and validated 2026-07-29. See `achievements.md`.
- [ ] Run the joint sampler on real Planck data; report the joint (C_ℓ, φ) posterior's internal lensing-consistency verdict.

## 3. Phase 3 — polarization / LiteBIRD delensing (the science paper)

Full TQU joint analysis; the reason curved-sky matters at all. After Phase 2 submits. Target-experiment reference: the LiteBIRD lensing forecast (arXiv:2507.22618, July 2025) — its pipeline is QE/iterative, so a sampling-based delensing result fills a real gap, not a redundant one (see `literature.md`).

- [ ] Spin-2 extension of alm utilities and the lensing operator (ducc0 spin-2 transforms — Phase 1.5 infrastructure carries over).
- [ ] TQU joint likelihood with (TT, TE, EE, BB) block; C_ℓ^TE breaks inverse-Gamma conjugacy → 2×2 inverse-Wishart (BeyondPlanck structure) or HMC.
- [ ] Simulated lensed TQU at LiteBIRD-like noise: delensing efficiency vs QE/iterative baselines; recovered r constraint.

## 4. Parked (do not start; recorded so the platform argument isn't lost)

- **Phase 2b — ΛCDM parameters from C_ℓ**: scientifically routine; a cheap robustness section *after* the Phase 2 paper submits.
- **Phase 4 — lmax≥1000 scaling**: tuning, not rearchitecture, now the dense matrix is gone; profile only when Phase 2/3 need it.
- **Phase 5 — non-Gaussian extensions** (fNL sampling, mask in-painting, learned priors, systematics blocks): each a separate paper after Phases 2–3.
- **Lensed-operator exact Block-2 draw**: gate 2 (Section 1) failed because `sample_alm_cg` was calibrated against the *unlensed* operator `Y` while the data pass through `L(φ)∘Y` — the 652% bias is a calibration mismatch, not proof that no exact draw can work. A CG solve built from autodiff of `psi_lensed` (mirroring `_cg_grad_fn`'s use of `_psi_tf_raw`) would target the correct conditional exactly; the open question is whether the diagonal preconditioner still near-diagonalises `(L∘Y)^T N^{-1} (L∘Y)` well enough for fast PCG convergence, since lensing breaks the ℓ-diagonal structure the current preconditioner assumes. Not worth pursuing unless HMC-on-both-blocks (the current path) becomes a proven throughput bottleneck at lmax=300 — gate 1 passed MARGINAL, so no urgency.
- **Re-tune the matrix-free-HMC step-size adaptation** (Section 0, not blocking): the accepted pivot's adapted step-size regime (0.16, accept 0.55-0.65) mixes ~4x less efficiently per-sample than the old dense-SHT reference's (0.10-0.11, accept 0.6-0.82); recovering that would make the pivot's already-large (~31x) net throughput win even larger. Skip unless Phase 2 chains show it matters.

## Standing discipline

- **One critical path** (re-set 2026-07-29): Phase 2 gates ✓ → **coverage/rank test at lmax≈128** → **joint-posterior differentiator figures** → paper. The lmax=300 point-validation run and the real-data demo are supporting evidence, not the critical path; scale is explicitly *not* the route to impact. Anything not on this waits.
- **Demonstrated beats asserted.** Against a learned-inference competitor, "asymptotically exact" is only worth what the convergence evidence backs. Prefer a converged result at a smaller scale over a non-converged result at a larger one — every time, and say which one you have.
- **Precision rule**: fp64 end-to-end unless a mixed scheme with fp64 accumulation is validated against fp64 chains (the float32 false convergence is the standing counterexample).
- **Dense-reference discipline**: every new sampler/operator is validated against an exact small-scale reference before production — this has caught five real bugs so far (see `achievements.md`); don't skip it under time pressure.
- **Claims hygiene**: every "first" carries scope qualifiers and nearest-prior-work citations (Millea+2020, MUSE, delensalot); re-check arXiv for curved-sky MUSE before each submission milestone.
- **R-hat on C_ℓ alone is not convergence** — chains drifting together from the same start fool it; check the alm block and tail-ESS trends too (the messenger post-mortem's lesson).
- On dine2/cosma8 reused nodes, scripts need a job-private `$TMPDIR` (autograph cache collision).
- **`tf.gather` on a `tf.custom_gradient` output produces `IndexedSlices`, not a dense tensor**, as the upstream cotangent — any downstream `tf.py_function`/`.numpy()` call in that custom gradient's backward pass must `tf.convert_to_tensor()` it first (bit us in `apply_lensing_tf`'s backward when the matrix-free masked-likelihood path added a `tf.gather` on its output — see `lensing.py`, commit `19a7977`). Applies to any future custom-gradient op fed into `tf.gather`/`tf.boolean_mask`.
